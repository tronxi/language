load memory .
fmod TYPE is

    pr MEMORY .
    sort Type .

    subsorts Qid Int < Type .

    var Q : Qid .
    var M : Memory .
    var V : Int .
    op load : Memory Type -> Type .
    eq load(M, Q) = find(M, Q) .
    eq load(M, V) = V .

endfm